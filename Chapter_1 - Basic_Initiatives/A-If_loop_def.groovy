boolean i= true
if (i)
msg = "'If' check passed" // Default message to send
else 
msg = "'Else' check passed "    
println "MESSAGE: "+msg

/*
INFO:
Here `msg` variable assigned without any global `def` and working as global if applied inside in `if`-`else` Loop
*/
